spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cvmanage
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  application:
    name: inventory
